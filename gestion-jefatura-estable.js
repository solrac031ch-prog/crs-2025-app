(() => {
  if ("serviceWorker" in navigator) {
    navigator.serviceWorker.getRegistrations?.().then((items) => items.forEach((item) => item.unregister())).catch(() => {});
  }
  if (window.caches?.keys) {
    caches.keys().then((keys) => keys.forEach((key) => caches.delete(key))).catch(() => {});
  }

  const USERS_KEY = "crsChiefUsersV2";
  const SESSION_KEY = "crsChiefSessionV2";
  const LINKS_KEY = "crsOperationalLinksStable";
  const CONTENT_KEY = "crsContentHubStable";
  const PHONES_KEY = "crsExtraPhonesStable";
  const FORMS_KEY = "crsFormsLinksStable";
  const ONCALL_KEY = "crsOnCallCsvStable";
  const ARSENAL_KEY = "crsArsenalCsvStable";
  const ADMINS = { desarrollador: "Dr Carlos Herrera", guti: "Dr Pablo Gutierrez", cote: "Dra Maria-Jose Marin" };

  const $ = (selector, root = document) => root.querySelector(selector);
  const $$ = (selector, root = document) => Array.from(root.querySelectorAll(selector));
  const clean = (value) => String(value || "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase().replace(/[^a-z0-9]/g, "");
  const esc = (value) => String(value || "").replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#039;");
  const read = (key, fallback) => { try { return JSON.parse(localStorage.getItem(key) || "") || fallback; } catch { return fallback; } };
  const write = (key, value) => localStorage.setItem(key, JSON.stringify(value));
  const users = () => read(USERS_KEY, []);
  const session = () => read(SESSION_KEY, null);
  const isAdmin = (user = session()) => Boolean(user && (["owner", "desarrollador"].includes(user.role) || Object.prototype.hasOwnProperty.call(ADMINS, clean(user.username))));

  async function hashPassword(username, password) {
    const text = `${String(username || "").trim().toLowerCase().replace(/\s+/g, "")}::${String(password || "")}`;
    if (crypto?.subtle && window.TextEncoder) {
      const bytes = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(text));
      return Array.from(new Uint8Array(bytes)).map((item) => item.toString(16).padStart(2, "0")).join("");
    }
    return btoa(unescape(encodeURIComponent(text)));
  }

  function addStyle() {
    if ($("#gestion-jefatura-estable-style")) return;
    const style = document.createElement("style");
    style.id = "gestion-jefatura-estable-style";
    style.textContent = `.page:not(.active){display:none!important}#chiefPage>.page-head{display:none!important}.stable-shell{display:grid;gap:14px}.stable-hero{position:relative;overflow:hidden;display:grid;gap:12px;padding:clamp(20px,4vw,34px);border-radius:14px;background:linear-gradient(135deg,#0f172a 0%,#14532d 48%,#0f766e 100%);color:#fff;box-shadow:0 24px 60px rgba(15,23,42,.24)}.stable-hero:after{content:"";position:absolute;right:-70px;bottom:-95px;width:250px;height:250px;border-radius:50%;background:rgba(255,255,255,.12)}.stable-hero h2{font-size:clamp(2rem,5vw,3.7rem);line-height:1;margin:0;color:#fff}.stable-hero p{max-width:780px;color:#def7ef;line-height:1.5;margin:0}.stable-actions{display:flex;flex-wrap:wrap;gap:10px;position:relative;z-index:1}.stable-actions .document-button:first-child{background:#f59e0b;border-color:#f59e0b;color:#1f1300}.stable-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));gap:12px}.stable-card{display:grid;gap:8px;padding:16px;border:1px solid #dfe8e4;border-left:6px solid #0f766e;border-radius:12px;background:#fff;box-shadow:0 12px 30px rgba(15,23,42,.08)}.stable-card.amber{border-left-color:#f59e0b}.stable-card.blue{border-left-color:#2563eb}.stable-card.purple{border-left-color:#7c3aed}.stable-card strong{font-size:1.08rem;color:#10201c}.stable-card span,.stable-card p{color:#52615c;line-height:1.42;margin:0}.stable-admin{display:grid;gap:14px}.stable-admin-section{display:grid;gap:10px}.stable-table{width:100%;border-collapse:collapse;min-width:760px}.stable-table th,.stable-table td{padding:9px 10px;border-bottom:1px solid #edf0ed;text-align:left;vertical-align:top}.stable-table th{background:#f6f8f7;font-size:.78rem;text-transform:uppercase;color:#44504b}.stable-table-wrap{overflow:auto;border:1px solid #dfe8e4;border-radius:10px;background:#fff}.stable-note{padding:12px;border:1px solid #f4d28a;border-radius:10px;background:#fff7e8;color:#664100;font-weight:750}.chief-form{display:grid;gap:10px}.chief-form label{display:grid;gap:6px;color:#24312d;font-weight:850}.chief-form input,.chief-form select,.chief-form textarea{width:100%;min-height:42px;padding:9px 11px;border:1px solid #cbd5d1;border-radius:8px;background:#fff;color:#10201c}.admin-list{display:grid;gap:8px}.admin-row{display:grid;gap:6px;padding:10px;border:1px solid #dfe8e4;border-radius:10px;background:#fbfdfc}@media(max-width:680px){.stable-actions{display:grid}.stable-actions .document-button{width:100%;justify-content:center}}`;
    document.head.append(style);
  }

  function activate(pageId) {
    $$(".page").forEach((page) => page.classList.toggle("active", page.id === pageId));
    $$("[data-route-link]").forEach((link) => link.classList.toggle("active", link.dataset.routeLink === (pageId === "managementPage" ? "gestion" : pageId === "chiefPage" ? "gestion" : "")));
    $("#chiefPage .page-head")?.remove();
  }

  function getCases() { try { if (typeof priorityCases === "function") return priorityCases(); } catch {} return read("crsPriorityCases", []); }
  function contentState() { return read(CONTENT_KEY, { education: [], news: [], paper: null }); }

  function renderGestion() {
    addStyle(); activate("managementPage");
    const title = $("#managementTitle"); if (title) title.textContent = "Gestion";
    const content = $("#managementContent"); if (!content) return;
    const state = contentState(); const paper = state.paper; const news = state.news?.[0];
    content.innerHTML = `<div class="stable-shell"><section class="stable-hero"><h2>Seguimiento local</h2><p>Tablero operativo para casos priorizados, planillas, educacion medica, paper del mes y noticias SUHPH.</p><div class="stable-actions"><a class="document-button" href="#/jefatura">Panel restringido Jefatura</a><a class="document-button" href="#/especialidades">Ver flujos</a><a class="document-button" href="#/educacion">Educacion medica</a><a class="document-button" href="#/inicio">Inicio</a><button class="document-button" type="button" data-stable-scroll="paper">Paper del mes</button><button class="document-button" type="button" data-stable-scroll="news">Noticias SUHPH</button></div></section><section class="stable-grid"><article class="stable-card"><strong>Casos guardados</strong><span>${getCases().length}</span></article><article class="stable-card amber"><strong>Planillas operativas</strong><span>Especialistas de llamado, UHD y visita diaria.</span></article><article class="stable-card blue"><strong>Accesos por rol</strong><span>Jefatura administra; turnos y subrogantes usan la app.</span></article></section><section class="stable-grid"><article class="stable-card purple" data-stable-section="paper"><strong>Paper del mes</strong><span>${esc(paper?.description || paper?.title || "Pendiente de publicar.")}</span>${paper?.url ? `<a href="${esc(paper.url)}" target="_blank" rel="noopener noreferrer">Abrir paper</a>` : ""}</article><article class="stable-card amber" data-stable-section="news"><strong>Noticias SUHPH</strong><span>${esc(news?.description || news?.title || "Cursos, congresos, actividades y avisos apareceran aqui.")}</span>${news?.url ? `<a href="${esc(news.url)}" target="_blank" rel="noopener noreferrer">Abrir noticia</a>` : ""}</article></section></div>`;
  }

  const nav = () => `<div class="route-actions"><a class="back-link" href="#/gestion">Volver</a><a class="back-link" href="#/inicio">Inicio</a></div>`;
  function authView() { const first = users().length === 0; return `${nav()}<section class="stable-hero"><h2>Panel restringido Jefatura</h2><p>Administracion de permisos, planillas, documentos y contenidos visibles en la app.</p></section><section class="document-panel"><div class="stable-grid"><article class="stable-card"><strong>Dr Carlos Herrera</strong><span>Desarrollador y administracion completa.</span></article><article class="stable-card"><strong>Dr Pablo Gutierrez</strong><span>Jefe de urgencia.</span></article><article class="stable-card"><strong>Dra Maria-Jose Marin</strong><span>Jefa de urgencia.</span></article></div><p data-chief-message></p>${first ? `<form class="chief-form" data-stable-bootstrap><label>Usuario autorizado<select name="username"><option value="desarrollador">Dr Carlos Herrera</option><option value="guti">Dr Pablo Gutierrez</option><option value="cote">Dra Maria-Jose Marin</option></select></label><label>Nombre<input name="name" required></label><label>Clave<input name="password" type="password" minlength="4" required></label><button class="document-button" type="submit">Crear primer acceso</button></form>` : `<form class="chief-form" data-stable-login><label>Usuario<input name="username" required></label><label>Clave<input name="password" type="password" required></label><button class="document-button" type="submit">Ingresar</button></form><form class="chief-form" data-stable-create-password><label>Usuario autorizado<input name="username" required></label><label>Clave temporal<input name="temporaryPassword" type="password" required></label><label>Nueva clave<input name="password" type="password" minlength="4" required></label><button class="document-button" type="submit">Crear clave</button></form>`}</section>`; }
  function casesTable() { const rows = getCases().map((item) => `<tr><td>${esc(new Date(item.createdAt || Date.now()).toLocaleString("es-CL"))}</td><td>${esc(item.status || "Pendiente")}</td><td>${esc(item.flow || item.protocol || "")}</td><td>${esc(item.patientName || "")}<br>${esc(item.rut || "")}<br>${esc(item.phone || "")}</td><td>${esc(item.summary || "")}</td><td>${esc(item.need || "")}</td></tr>`).join("") || `<tr><td colspan="6">No hay casos guardados.</td></tr>`; return `<section class="document-panel"><h2>Casos para gestion</h2><div class="stable-actions"><button class="document-button" data-export-stable="csv">Excel</button><button class="document-button" data-export-stable="pdf">PDF</button><button class="document-button" data-export-stable="word">Word</button></div><div class="stable-table-wrap"><table class="stable-table"><thead><tr><th>Fecha</th><th>Estado</th><th>Flujo</th><th>Paciente</th><th>Resumen</th><th>Necesita</th></tr></thead><tbody>${rows}</tbody></table></div></section>`; }
  function adminCenter() { const links = read(LINKS_KEY, {}); const forms = read(FORMS_KEY, {}); const phones = read(PHONES_KEY, []); const userRows = users().map((user) => `<div class="admin-row"><strong>${esc(user.name || user.username)}</strong><span>${esc(user.username)} · ${esc(user.role || "jefe")}</span>${Object.prototype.hasOwnProperty.call(ADMINS, clean(user.username)) ? "" : `<button class="document-button" data-remove-stable-user="${esc(user.username)}">Eliminar</button>`}</div>`).join(""); return `<section class="document-panel stable-admin"><h2>Centro de administracion</h2><p class="stable-note">Los datos quedan en este navegador. Para que sean compartidos por todos, se debe conectar Google Sheets/Drive publicado o Apps Script.</p><div class="stable-grid"><article class="stable-card stable-admin-section"><h2>Usuarios</h2><form class="chief-form" data-stable-user><label>Usuario<input name="username" required></label><label>Nombre<input name="name" required></label><label>Rol<select name="role"><option value="jefe-turno">Jefe de turno</option><option value="subrogante">Subrogante</option><option value="jefe">Jefe</option><option value="owner">Administrador</option></select></label><label>Clave temporal<input name="temporaryPassword" type="password" minlength="4" required></label><button class="document-button">Autorizar</button></form><div class="admin-list">${userRows}</div></article><article class="stable-card stable-admin-section"><h2>Planillas</h2><form class="chief-form" data-stable-links><label>Especialista de llamado<input name="llamados" type="url" value="${esc(links.llamados)}"></label><label>UHD<input name="uhd" type="url" value="${esc(links.uhd)}"></label><label>Visita diaria<input name="visita" type="url" value="${esc(links.visita)}"></label><label>CSV/TSV llamados<input name="onCallCsv" type="file" accept=".csv,.tsv,.txt"></label><button class="document-button">Actualizar</button></form></article><article class="stable-card stable-admin-section"><h2>Arsenal terapeutico</h2><form class="chief-form" data-stable-arsenal><label>CSV/TSV arsenal<input name="arsenalCsv" type="file" accept=".csv,.tsv,.txt"></label><button class="document-button">Actualizar arsenal</button></form></article><article class="stable-card stable-admin-section"><h2>Formularios de turno</h2><form class="chief-form" data-stable-forms><label>Medicamentos uso ocasional<input name="medicamentosUsoOcasionalUrl" type="url" value="${esc(forms.medicamentosUsoOcasionalUrl)}"></label><label>Ley de urgencias<input name="leyUrgenciasUrl" type="url" value="${esc(forms.leyUrgenciasUrl)}"></label><label>Notificacion obligatoria<input name="notificacionObligatoriaUrl" type="url" value="${esc(forms.notificacionObligatoriaUrl)}"></label><button class="document-button">Guardar formularios</button></form></article><article class="stable-card stable-admin-section"><h2>Directorio telefonico</h2><form class="chief-form" data-stable-phone><label>Nombre<input name="name" required></label><label>Detalle<input name="detail" required></label><label>Telefono<input name="phone" required></label><button class="document-button">Agregar telefono</button></form><div class="admin-list">${phones.map((phone, i) => `<div class="admin-row"><strong>${esc(phone.name)}</strong><span>${esc(phone.detail)} · ${esc(phone.phone)}</span><button class="document-button" data-remove-stable-phone="${i}">Eliminar</button></div>`).join("")}</div></article><article class="stable-card stable-admin-section"><h2>Educacion, paper y noticias</h2><form class="chief-form" data-stable-content><label>Tipo<select name="type"><option value="education">Educacion medica</option><option value="paper">Paper del mes</option><option value="news">Noticias SUHPH / cursos / actividades</option></select></label><label>Titulo<input name="title" required></label><label>Descripcion<textarea name="description" rows="3"></textarea></label><label>Link<input name="url" type="url"></label><button class="document-button">Publicar</button></form></article></div></section>`; }
  function renderJefatura() { addStyle(); activate("chiefPage"); const content = $("#chiefContent"); if (!content) return; const active = session(); content.innerHTML = active ? `${nav()}<section class="stable-hero"><h2>Panel restringido Jefatura</h2><p>Sesion: ${esc(active.name || active.username)} (${esc(active.role)})</p><div class="stable-actions"><button class="document-button" data-stable-logout>Cerrar sesion</button><a class="document-button" href="#/inicio">Inicio</a></div></section>${isAdmin(active) ? adminCenter() : ""}${casesTable()}` : authView(); }
  function applyLinks() { const links = read(LINKS_KEY, {}); const forms = read(FORMS_KEY, {}); try { if (typeof externalDocs !== "undefined") Object.assign(externalDocs, { llamadosUrl: links.llamados || externalDocs.llamadosUrl, uhdDisponibilidadUrl: links.uhd || externalDocs.uhdDisponibilidadUrl, visitaDiariaUrl: links.visita || externalDocs.visitaDiariaUrl }); } catch {} try { if (typeof externalForms !== "undefined") Object.assign(externalForms, forms); } catch {} }
  function readFile(file) { return new Promise((resolve) => { if (!file) return resolve(""); const reader = new FileReader(); reader.onload = () => resolve(String(reader.result || "")); reader.onerror = () => resolve(""); reader.readAsText(file); }); }

  document.addEventListener("submit", async (event) => { const form = event.target; if (!form.closest("[data-stable-bootstrap], [data-stable-login], [data-stable-create-password], [data-stable-user], [data-stable-links], [data-stable-arsenal], [data-stable-forms], [data-stable-phone], [data-stable-content]")) return; event.preventDefault(); const data = new FormData(form); if (form.matches("[data-stable-bootstrap]")) { const username = clean(data.get("username")); const user = { username, name: data.get("name") || ADMINS[username] || username, role: username === "desarrollador" ? "desarrollador" : "owner", hash: await hashPassword(username, data.get("password")) }; write(USERS_KEY, [user]); sessionStorage.setItem(SESSION_KEY, JSON.stringify(user)); renderJefatura(); return; } if (form.matches("[data-stable-login]")) { const typed = clean(data.get("username")); const alias = typed.includes("carlos") ? "desarrollador" : typed.includes("pablo") || typed.includes("gutierrez") ? "guti" : typed.includes("maria") || typed.includes("marin") || typed.includes("cote") ? "cote" : typed; const user = users().find((item) => clean(item.username) === alias || clean(item.name) === typed); if (user?.hash && await hashPassword(user.username, data.get("password")) === user.hash) { sessionStorage.setItem(SESSION_KEY, JSON.stringify(user)); renderJefatura(); } return; } if (form.matches("[data-stable-create-password]")) { const username = clean(data.get("username")); const list = users(); const index = list.findIndex((item) => clean(item.username) === username); if (index >= 0 && await hashPassword(username, data.get("temporaryPassword")) === list[index].temporaryHash) { list[index].hash = await hashPassword(username, data.get("password")); delete list[index].temporaryHash; write(USERS_KEY, list); sessionStorage.setItem(SESSION_KEY, JSON.stringify(list[index])); renderJefatura(); } return; } if (form.matches("[data-stable-user]")) { const username = clean(data.get("username")); const list = users().filter((item) => clean(item.username) !== username); list.push({ username, name: data.get("name"), role: data.get("role"), temporaryHash: await hashPassword(username, data.get("temporaryPassword")) }); write(USERS_KEY, list); renderJefatura(); return; } if (form.matches("[data-stable-links]")) { write(LINKS_KEY, { llamados: data.get("llamados"), uhd: data.get("uhd"), visita: data.get("visita") }); const csv = await readFile(form.onCallCsv.files[0]); if (csv) localStorage.setItem(ONCALL_KEY, csv); applyLinks(); renderJefatura(); return; } if (form.matches("[data-stable-arsenal]")) { const csv = await readFile(form.arsenalCsv.files[0]); if (csv) localStorage.setItem(ARSENAL_KEY, csv); renderJefatura(); return; } if (form.matches("[data-stable-forms]")) { write(FORMS_KEY, Object.fromEntries(data.entries())); applyLinks(); renderJefatura(); return; } if (form.matches("[data-stable-phone]")) { const phones = read(PHONES_KEY, []); phones.push({ name: data.get("name"), detail: data.get("detail"), phone: data.get("phone") }); write(PHONES_KEY, phones); renderJefatura(); return; } if (form.matches("[data-stable-content]")) { const state = contentState(); const item = { title: data.get("title"), description: data.get("description"), url: data.get("url") }; if (data.get("type") === "paper") state.paper = item; else state[data.get("type")].unshift(item); write(CONTENT_KEY, state); renderJefatura(); return; } }, true);
  document.addEventListener("click", (event) => { if (event.target.closest("[data-stable-logout]")) { sessionStorage.removeItem(SESSION_KEY); renderJefatura(); } const scroll = event.target.closest("[data-stable-scroll]"); if (scroll) $(`[data-stable-section="${scroll.dataset.stableScroll}"]`)?.scrollIntoView({ behavior: "smooth", block: "center" }); const removeUser = event.target.closest("[data-remove-stable-user]"); if (removeUser) { write(USERS_KEY, users().filter((u) => clean(u.username) !== clean(removeUser.dataset.removeStableUser))); renderJefatura(); } const removePhone = event.target.closest("[data-remove-stable-phone]"); if (removePhone) { const phones = read(PHONES_KEY, []); phones.splice(Number(removePhone.dataset.removeStablePhone), 1); write(PHONES_KEY, phones); renderJefatura(); } }, true);
  function route() { applyLinks(); addStyle(); $("#chiefPage .page-head")?.remove(); const hash = location.hash.split("?")[0]; if (hash === "#/gestion") renderGestion(); if (hash === "#/jefatura") renderJefatura(); }
  addEventListener("hashchange", route); setTimeout(route, 80);
})();